For being able to work on this project:

1) clone it on your pc
2) install nodejs
3) install polymer cli (npm install -g polymer-cli)
3) install lit-element (npm install lit-element)
4) install webcomponents (npm install @webcomponents/webcomponentsjs)
5) serve the FoodEx2 Web Component locally (polymer serve)